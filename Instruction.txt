📝 Guide de Démonstration (Antisèche)

Suis ces étapes dans l'ordre pour une présentation fluide sans bugs.

ÉTAPE 0 : Préparation (Avant que le prof arrive)

Ouvre VS Code.

Terminal 1 : Lance la blockchain.
npx hardhat node
(Ne ferme jamais ce terminal !)

Terminal 2 : Déploie le contrat.
npx hardhat run scripts/deploy.js --network localhost

Mise à jour des fichiers :

Copie l'adresse générée (ex: 0x5Fb...).

Colle-la dans index.html, ecole.html, et entreprise.html (Ligne const contractAddress = "...").

Sauvegarde tout (Ctrl+S).

ÉTAPE 1 : Le Ministère (Admin)

Scénario : "Je suis le Ministère et je veux accréditer une école (ESCP)."

Ouvre index.html dans le navigateur.

Tu as besoin de l'adresse de la future école.

Va dans ton Terminal 1 (celui qui défile).

Remonte au début pour voir la liste "Accounts".

Copie l'adresse du Account #1 (ex: 0x7099...).

Retourne sur la page Admin.

Colle l'adresse dans le champ "Adresse de l'école".

Mets le nom "ESCP".

Clique sur ✅ Autoriser cette École.

Valide le message de succès.

ÉTAPE 2 : L'École (ESCP)

Scénario : "Je suis l'administration de l'ESCP. Je me connecte et je crée un diplôme pour un étudiant."

Ouvre ecole.html dans le navigateur.

Montre que tu es connecté en tant que 0x7099... (C'est automatique dans le code).

Entre un nom : "Saad" (ou le nom du prof).

Entre un diplôme : "Master Blockchain".

Clique sur ✍️ Signer et Créer.

Un code s'affiche en bas (ex: 0x8a7...).

Copie ce code.

Dis : "J'envoie ce code par mail à l'étudiant."

ÉTAPE 3 : Le Recruteur (Entreprise)

Scénario : "Je suis une entreprise, je reçois le CV de Saad et je veux vérifier si son diplôme est vrai."

Ouvre entreprise.html dans le navigateur.

Colle le code que tu viens de copier.

Clique sur Vérifier.

Résultat attendu : ✅ DIPLÔME VALIDE (En vert).

Montre les détails (Nom, Date, École).

ÉTAPE 4 : Le "Crash Test" (Bonus Révocation)

Scénario : "L'école ESCP a fraudé. Le ministère décide de la fermer. Que devient le diplôme ?"

Retourne sur la page index.html (Admin).

Vérifie que l'adresse de l'ESCP est toujours dans le champ.

Clique sur ⛔ Bloquer cette École.

Retourne sur la page entreprise.html.

Clique à nouveau sur Vérifier avec le même code.

Résultat attendu : ⚠️ VALIDE (Mais école fermée) (En orange).

Conclusion : "Le diplôme reste authentique (l'étudiant l'a bien eu), mais on signale au recruteur que l'école n'est plus fiable."

💡 Astuce en cas de panique

Si plus rien ne marche ou si tu as une erreur bizarre :

Ferme les terminaux (Poubelle).

Relance npx hardhat node.

Relance le déploiement.

Change l'adresse dans les HTML.

Rafraîchis les pages web.